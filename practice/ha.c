#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void){
char * ptrs;
ptrs = new char[5];
int i = 0;
  puts("HI");
  ptrs[0] = 0;
  i++;
  
if(ptrs == NULL)
 puts("HE");

return 0;
}
